<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="admin">
	<!-- 회원 리스트 -->
	<select id="memberList" resultType="com.company.spring.member.MemberVO">
	SELECT mid, mname, mmail, menter, mvalue
	FROM member_tb
	</select>
	<!-- 게시물 리스트 -->
	<select id="boardList" resultType="com.company.spring.board.BoardVO">
	SELECT bidx, bwriter, btitle, bcontent, bdate, bvalue
	FROM board_tb
	</select>
	<!-- 댓글 리스트 -->
	<select id="replyList" resultType="com.company.spring.reply.ReplyVO">
	SELECT ridx, rcontent, rwriter, rdate, rvalue
	FROM reply_tb
	</select>
	<!-- 상품 업로드 -->
	<insert id="upload">
		INSERT INTO product_tb
			(pidx, pname, pprice, pinfo, pimage, pthumbimg)
		VALUES (#{pidx}, #{pname}, #{pprice}, #{pinfo}, #{pimage}, #{pthumbimg})	
	</insert>
	<!-- 상품 수정 페이지 -->
	<select id="modify" resultType="com.company.spring.product.ProductVO">
		SELECT *
		FROM product_tb
		WHERE pidx = #{pidx}
	</select>
	<!-- 상품 수정 -->
	<update id="productModify">
		UPDATE product_tb
		SET pname=#{pname}, pprice=#{pprice}, pinfo=#{pinfo}, pimage=#{pimage}, pthumbimg=#{pthumbimg}
		WHERE pidx = #{pidx}
	</update>
	<!-- 상품 삭제 -->
	<delete id="productDelete">
		DELETE 
		FROM product_tb
		WHERE pidx = #{pidx}
	</delete>
</mapper>